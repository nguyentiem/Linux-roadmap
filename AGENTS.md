# Quy ước khi diễn giải bài học

Khi người dùng yêu cầu giải thích hoặc diễn giải một bài trong `bai-hoc/`:

1. Chỉ cập nhật file của bài được nêu, trừ khi người dùng yêu cầu rõ thêm file khác. Viết nội dung diễn giải vào file; trong chat chỉ báo ngắn gọn kết quả, không lặp lại bài giảng.
2. Trước khi viết, kiểm tra các kiến thức kỹ thuật trên mạng. Ưu tiên tài liệu chính thức, tài liệu kernel/distro và nguồn gốc của công cụ; đối chiếu những điểm có thể khác theo phiên bản hoặc môi trường. Gắn liên kết nguồn phù hợp trong chính bài học.
3. Giải thích bằng tiếng Việt đơn giản, bắt đầu từ khái niệm cơ bản rồi dẫn từng bước đến cơ chế và ví dụ thực tế. Định nghĩa thuật ngữ trước khi dùng; phân biệt rõ ví dụ minh họa với kết quả quan sát thật.
4. Thêm sơ đồ văn bản hoặc Mermaid khi nó giúp thấy quan hệ giữa các thành phần hay đường đi của dữ liệu. Mỗi sơ đồ cần có phần giải thích để người mới đọc hiểu được.
5. Giữ cấu trúc bài học dễ theo dõi: mục tiêu → khái niệm → cơ chế → ví dụ → lab và cách đọc kết quả → lỗi thường gặp → tự kiểm tra. Rà lại tính đúng đắn, liên kết và định dạng sau khi sửa.
6. Dùng `refer.md` làm **mốc về độ sâu diễn giải**: không dừng ở định nghĩa ngắn. Với mỗi ý lớn, giải thích nó là gì, vì sao cần, cách hoạt động, một ví dụ cụ thể, cách tự kiểm tra và giới hạn của ví dụ. Bao phủ đầy đủ các ý quan trọng của bài gốc và mở rộng những bước người mới thường phải tự đoán. Không sao chép nguyên văn hoặc giữ các ví dụ/số phiên bản trong `refer.md` nếu chưa kiểm chứng.
7. Sắp xếp bài như một chuỗi câu hỏi tự nhiên của người mới học. Đi từ toàn cảnh đến từng thành phần, rồi nối chúng bằng một tình huống xuyên suốt. Sơ đồ, bảng và lệnh phải có lời dẫn và cách đọc; tránh chỉ liệt kê thuật ngữ. Cuối bài có tóm tắt ngắn để người học tự nhắc lại mô hình tư duy.
